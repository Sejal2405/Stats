# Experiment 2

Files:
- `exp2.py` — Main experiment script for Experiment 2.
- `diabetes.csv` — Dataset used by the experiment (diabetes dataset).

Description:
This folder contains an experiment that uses `diabetes.csv` to train and evaluate a model. The script likely includes data loading, feature preparation, modeling, and evaluation steps.

Usage:

```
pip install pandas numpy scikit-learn
python exp2.py
```

Notes:
- Edit `exp2.py` to change model parameters or paths.
