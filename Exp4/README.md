# Experiment 4

Files:
- `exp4.py` — Main experiment script for Experiment 4.
- `diabetes.csv` — Dataset used by the experiment (diabetes dataset).

Description:
This folder holds Experiment 4 which works with the diabetes dataset. Expect data loading, feature engineering, model training, and evaluation steps.

Usage:

```
pip install pandas numpy scikit-learn
python exp4.py
```

Notes:
- Update `exp4.py` if you need different model hyperparameters or file paths.
